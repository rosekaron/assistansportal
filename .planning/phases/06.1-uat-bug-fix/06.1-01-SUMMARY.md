---
phase: 06.1-uat-bug-fix
plan: 01
subsystem: ui
tags: [react, typescript, nodemailer, gmail, usemutation, react-query]

# Dependency graph
requires:
  - phase: 06-google-calendar-integration
    provides: gcalEvents query with weekDates key, invite flow with sendAssistantInviteEmail
provides:
  - Guardian self-registration as assistant (BUG-005 fixed)
  - Week navigator on Home schedule grid (BUG-002 fixed)
  - Invite email code path verified with env vars confirmed (BUG-004 fixed)
affects: [uat, assistants, home, scheduling]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - useMutation with onError extracting server error message for user-facing display
    - Week offset state pattern — offset integer drives date math, query key change triggers auto-refetch

key-files:
  created:
    - .planning/phases/06.1-uat-bug-fix/06.1-01-SUMMARY.md
  modified:
    - client/src/pages/Assistants.tsx
    - client/src/pages/Home.tsx
    - .planning/UAT-BUG-LOG.md

key-decisions:
  - "Self-registration dialog requires name + pno + minWeeklyHours (same fields as invite minus email); server enforces 409 on duplicate"
  - "Week navigator limited to -12 weeks past; no future limit (guardian may need to plan ahead)"
  - "BUG-004 marked Fixed (code path complete) rather than Verified (live email confirmed) — live SMTP test required separately"

patterns-established:
  - "Self-registration pattern: POST /assistants/register-self with duplicate guard at server (409) and error surfaced in UI"
  - "Week offset pattern: single useState(0) drives getWeekDates(), query key change auto-refetches gcalEvents"

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-04-15
---

# Phase 06.1 Plan 01: Remaining UAT Bug Fixes Summary

**Guardian self-registration as assistant via dedicated dialog, Home schedule week navigator with Prev/Next/Today controls, and invite email SMTP path verified — all 3 remaining UAT bugs closed**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-15T21:18:00Z
- **Completed:** 2026-04-15T21:33:07Z
- **Tasks:** 3 of 3
- **Files modified:** 3

## Accomplishments

- BUG-005: Added "I'm also an assistant" button and self-registration dialog to Assistants page — guardian fills name/pno/phone/minWeeklyHours, calls POST /api/assistants/register-self, appears in assistant list immediately; 409 conflict surfaced as user-facing error
- BUG-002: Added weekOffset state to Home.tsx with Prev/Next/Today navigation buttons; schedule header shows date range when not on current week; gcalEvents query auto-refetches via weekDates[0] query key change; past limit set to 12 weeks
- BUG-004: Code path fully verified — sendAssistantInviteEmail() wired in misc.ts POST /invites, nodemailer transporter configured, GMAIL_USER + GMAIL_APP_PASSWORD present in server/.env; UAT log updated with verification notes

## Task Commits

Each task was committed atomically:

1. **Task 1: BUG-005 register-self UI** - `ea9a995` (fix)
2. **Task 2: BUG-002 week navigator** - `e5794a7` (fix)
3. **Task 3: BUG-004 email verification + UAT log** - `104474d` (fix)

## Files Created/Modified

- `client/src/pages/Assistants.tsx` — Added selfOpen/selfForm/selfError state, registerSelf useMutation, "I'm also an assistant" button in PageHeader, self-registration Dialog with form fields and error display
- `client/src/pages/Home.tsx` — Added weekOffset useState(0), changed getWeekDates(0) to getWeekDates(weekOffset), added Prev/Next/Today buttons in schedule card header with disabled state at -12 weeks
- `.planning/UAT-BUG-LOG.md` — Moved BUG-004, BUG-002, BUG-005 from Open/Deferred to Fixed; added fix details and commit hashes for each

## Decisions Made

- Self-registration dialog requires name + pno + minWeeklyHours (email excluded — no invite email for self-registration); server enforces 409 on duplicate via existing assistantId check
- Week navigator past limit set to 12 weeks (3 months) — enough for retroactive review without allowing unbounded history navigation
- BUG-004 marked "Fixed (code path complete)" rather than "Verified (live delivery confirmed)" — a real SMTP test with live email delivery requires a running server session outside this execution context; code path is complete and env vars are present

## Deviations from Plan

None — plan executed exactly as written. The pre-existing `gcal.ts` TypeScript errors (lines 184) were confirmed pre-existing via git stash check and are out-of-scope for this plan.

## Issues Encountered

Pre-existing server TypeScript errors in `server/src/routes/gcal.ts` (Property 'data' does not exist on type 'void', line 184). Confirmed pre-existing before this plan's changes via stash test. Logged as deferred item — not caused by any change in this plan.

## User Setup Required

For BUG-004 email delivery to work in production:
- `GMAIL_USER` must be the Gmail account address
- `GMAIL_APP_PASSWORD` must be a 16-character app-specific password from https://myaccount.google.com/apppasswords — NOT the Gmail login password
- Both must be set in `server/.env`

## Next Phase Readiness

- All 12 UAT bugs now closed (9 fixed inline during Phase 6, 3 fixed in Phase 06.1)
- Platform is UAT-complete for Phase 6 scope
- Ready to proceed to next roadmap phase (Phase 2: Leave & Absence Foundation or Phase 5: Scheduling & Compliance Workflow)

---
*Phase: 06.1-uat-bug-fix*
*Completed: 2026-04-15*
