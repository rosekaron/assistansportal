---
phase: "02.5"
plan: "04"
subsystem: "client/pages"
tags: ["ui-overhaul", "assistant-dashboard", "light-mode", "b2b-design", "week-strip", "action-banners"]
dependency_graph:
  requires:
    - "02.5-02 — B2B colour token update (working palette)"
    - "02.5-03 — Guardian Layout & Dashboard redesign (established light-theme patterns)"
  provides:
    - "polished-assistant-dashboard"
  affects:
    - "client/src/pages/AssistantDashboard.tsx"
tech_stack:
  added: []
  patterns:
    - "bg-muted/30 for page background (off-white, differentiates from card surfaces)"
    - "bg-primary/10 ring-1 ring-primary/30 for today-cell highlight — matches guardian dashboard pattern"
    - "bg-white border border-border for non-today day cells — clean card surface"
    - "amber-50/amber-200 border for proposals banner — light amber tint, not dark moody"
    - "primary/5 + primary/20 border for reports banner — brand-blue tint"
key_files:
  created: []
  modified:
    - client/src/pages/AssistantDashboard.tsx
decisions:
  - "Logo icon: person-group SVG (w-9 h-9 rounded-xl bg-primary) — identical to guardian sidebar for brand consistency"
  - "Week hours colour: text-primary (not text-blue-400) — uses design token, not hardcoded blue"
  - "Day cell non-today: bg-white border border-border instead of bg-secondary/30 — gives each cell a white card surface"
  - "Action banner proposals: amber-50/amber-200 light tint — removed dark bg-amber-950/20 moody treatment"
  - "Action banner reports: primary/5 + primary/20 — brand-tied, replaces dark bg-blue-950/20"
  - "Tab count badge: bg-amber-100 text-amber-700 — removed bg-amber-900 text-amber-400 dark-mode badge"
  - "Shift card date: font-semibold (bump from font-medium) and text-foreground explicit — stronger typographic hierarchy"
  - "Time range in upcoming/slots: font-bold text-foreground (was font-semibold, no explicit colour) — cleaner"
metrics:
  duration: "< 10 minutes"
  completed: "2026-04-08"
  tasks_completed: 1
  tasks_total: 2
  files_changed: 1
---

# Phase 2.5 Plan 04: Assistant Dashboard Redesign Summary

**One-liner:** Replaced all dark-mode colour classes in AssistantDashboard.tsx with light-mode equivalents — amber-50 banners, primary-token week hours, white day cells, brand-icon header — matching the professional B2B aesthetic of the guardian view.

---

## What Was Built

### Task 1 — AssistantDashboard.tsx full light-mode conversion

All changes are JSX/className only. Imports, React Query hooks, mutations, derived state (`upcoming`, `pending`, `approved`, `needsReport`, `thisWeek`, `weekHours`), state variables (`tab`, `weekOffset`), and all business logic are unchanged.

| Area | Before | After | Reason |
|------|--------|-------|--------|
| Page background | `bg-background` | `bg-muted/30` | Off-white page differentiates from card surfaces |
| Header logo | `w-2 h-2 rounded-full bg-primary` | `w-9 h-9 rounded-xl bg-primary` + person-group SVG | Consistent with guardian sidebar branding |
| Header product name | `font-semibold` | `font-bold text-foreground` | Stronger product identity |
| Header pending count | `text-amber-400` | `text-amber-600` | Light-mode readable amber |
| Header reports count | `text-blue-400` | `text-primary` | Design system token, not hardcoded blue |
| Week hours value | `text-blue-400` | `text-primary` | Brand-tied, consistent with guardian dashboard |
| Today day cell | `bg-primary/15 ring-1 ring-primary/30` | `bg-primary/10 ring-1 ring-primary/30` | Cleaner opacity |
| Non-today day cell | `bg-secondary/30` | `bg-white border border-border` | White card surface per plan spec |
| Day hours text | `text-emerald-400` | `text-emerald-600` | Light-mode readable green |
| Pending dot | `bg-amber-400` | `bg-amber-500` | Slightly more saturated for visibility |
| Proposals banner | `border-amber-900/50 bg-amber-950/20 hover:border-amber-700/70` (dark) | `border-amber-200 bg-amber-50 hover:bg-amber-100` (light) | Full dark-mode elimination |
| Proposals banner title | `text-amber-300 font-medium` | `text-amber-800 font-semibold` | Light-mode readable, stronger hierarchy |
| Proposals banner subtitle | `text-muted-foreground` | `text-amber-600` | Brand-consistent amber |
| Proposals count badge | `text-amber-400 bg-amber-950 border-amber-900` | `text-amber-700 bg-amber-100 border-amber-200` | Light mode |
| Reports banner | `border-blue-900/50 bg-blue-950/20 hover:border-blue-700/70` (dark) | `border-primary/20 bg-primary/5 hover:bg-primary/10` (light) | Brand-tied, no hardcoded blue |
| Reports banner title | `text-blue-300 font-medium` | `text-primary font-semibold` | Design token, stronger hierarchy |
| Reports banner subtitle | `text-muted-foreground` | `text-muted-foreground` | Unchanged |
| Reports count badge | `text-blue-400 bg-blue-950 border-blue-900` | `text-primary bg-primary/10 border-primary/20` | Light mode / design token |
| Tab count badge | `bg-amber-900 text-amber-400` | `bg-amber-100 text-amber-700` | Full dark-mode elimination |
| Upcoming card date | `text-sm font-medium` | `text-sm font-semibold text-foreground` | Stronger hierarchy, explicit colour |
| Upcoming card time | `font-mono text-lg font-semibold` | `font-mono text-lg font-bold text-foreground` | Stronger, explicit |
| Proposals card date | `text-sm font-medium` | `text-sm font-semibold text-foreground` | Consistent with upcoming |
| Reports card date | `text-sm font-medium` | `text-sm font-semibold text-foreground` | Consistent |
| Open slots card date | `text-sm font-medium` | `text-sm font-semibold text-foreground` | Consistent |
| Open slots card time | `font-mono text-lg font-semibold` | `font-mono text-lg font-bold text-foreground` | Consistent |

---

## Task Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Redesign AssistantDashboard.tsx — light-mode colours, logo icon, banners | 5c5088a | client/src/pages/AssistantDashboard.tsx |
| 2 | Checkpoint: human-verify visual result | APPROVED | — |

---

## Checkpoint Status

**APPROVED** — human verified visual result at checkpoint. All dark-mode colours confirmed eliminated; light-theme consistent with guardian view. No iterations required.

---

## Deviations from Plan

None — all changes followed the plan specification exactly. Minor additions:
- Added `text-foreground` explicit colour to shift card date/time text (plan implied it, did not spell it out — this is correct semantic behaviour, not a deviation).

---

## Known Stubs

None. All changes are visual-only — no data wiring altered, no placeholder content introduced.

---

## Threat Flags

None. Only JSX className strings were modified. No network endpoints, auth paths, file access, or schema changes introduced. Threat register dispositions T-2.5-04-01 and T-2.5-04-02 both accepted — no new attack surface.

---

## Self-Check

```
FOUND: client/src/pages/AssistantDashboard.tsx — modified, committed as 5c5088a ✓
TypeScript: npx tsc --noEmit → 0 errors, exit 0 ✓
Dark-mode classes scan (amber-950, amber-900, blue-950, blue-900, blue-300, blue-400, text-amber-400, text-amber-300, bg-secondary/30, text-emerald-400, bg-primary/15): NONE FOUND ✓
text-primary for week hours: PRESENT ✓
bg-amber-50 for proposals banner: PRESENT ✓
bg-primary/5 for reports banner: PRESENT ✓
bg-amber-100 text-amber-700 for tab count badge: PRESENT ✓
w-9 h-9 rounded-xl person-group SVG in header: PRESENT ✓
```

## Self-Check: PASSED
