---
phase: 02.5-ui-overhaul
plan: 04
type: execute
wave: 3
depends_on:
  - 02.5-02-PLAN.md
files_modified:
  - client/src/pages/AssistantDashboard.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Assistant dashboard header clearly identifies the assistant and patient — readable at a glance"
    - "Week strip communicates the current week's shifts with a clear today highlight"
    - "Action banners for pending proposals and due reports are visually urgent but not alarming"
    - "Tab navigation is clean — the active tab is immediately obvious"
    - "Shift cards in each tab have clear typographic hierarchy: date, time, activity, status"
    - "The overall aesthetic is light and clean — matching the guardian view's care.com direction"
    - "Status indicators (pending amber, approved green, report-due blue) remain semantically correct"
  artifacts:
    - path: "client/src/pages/AssistantDashboard.tsx"
      provides: "Redesigned assistant dashboard with polished header, week strip, action banners, tabs, and shift cards"
      min_lines: 250
  key_links:
    - from: "client/src/pages/AssistantDashboard.tsx"
      to: "/api/assistant-self/*"
      via: "assistantSelfApi.* React Query hooks"
      pattern: "assistantSelfApi\\."
---

<objective>
Redesign the AssistantDashboard page — header, week strip, action banners, tab navigation, and shift
cards — to match the B2B professional standard established in Plans 02 and 03.

Purpose: The assistant view is the day-to-day interface for care workers. It must be clean, fast to
scan, and visually polished — not a developer sketch. Per D-05 and D-06.

Output: Redesigned AssistantDashboard.tsx. Routes, data fetching, mutations, and business logic unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.5-ui-overhaul/02.5-RESEARCH.md
@.planning/phases/02.5-ui-overhaul/02.5-CONTEXT.md
@.planning/phases/02.5-ui-overhaul/02.5-02-SUMMARY.md
@client/src/pages/AssistantDashboard.tsx
</context>

<interfaces>
<!-- Contracts that must not change — data and mutations must remain identical -->

From client/src/pages/AssistantDashboard.tsx:
```tsx
// Keep all React Query hooks exactly:
const { data: me }           = useQuery({ queryKey: ["assistant-me"],      ... });
const { data: entries = [] } = useQuery({ queryKey: ["assistant-entries"], ... });
const { data: slots = [] }   = useQuery({ queryKey: ["assistant-slots"],   ... });

// Keep all mutations exactly:
const accept      = useMutation({ mutationFn: (id) => assistantSelfApi.accept(id), ... });
const reject      = useMutation({ mutationFn: (id) => assistantSelfApi.reject(id), ... });
const submitReport= useMutation({ mutationFn: (id) => assistantSelfApi.submitReport(id), ... });
const selfBook    = useMutation({ mutationFn: (slotId) => assistantSelfApi.selfBook(slotId), ... });

// Keep all derived state calculations:
const upcoming, pending, approved, needsReport, thisWeek, weekHours
// These are correct — only JSX and className strings change.

// Keep logout and navigate usage exactly as-is.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign AssistantDashboard.tsx — header, week strip, banners, tabs, shift cards</name>
  <files>client/src/pages/AssistantDashboard.tsx</files>
  <action>
    Rewrite ONLY the JSX and className strings in AssistantDashboard.tsx. All imports, React Query
    hooks, mutations, calculated values, state variables (tab, weekOffset), and business logic remain
    unchanged.

    **IMPORTANT:** The current design uses dark-mode colour classes (bg-amber-950/20, border-amber-900/50,
    text-amber-300, text-blue-300, bg-blue-950, text-blue-400). This is the WRONG direction — the app
    is a light theme. Replace all dark-mode tinted classes with their light-mode equivalents.

    Section-by-section targets:

    **Outer container:**
    - Change `min-h-screen bg-background` to `min-h-screen bg-muted/30` — subtle off-white page
      background differentiates it from card surfaces

    **Header (sticky top bar):**
    - Keep `border-b border-border bg-card sticky top-0 z-10` — correct surface and positioning
    - Logo dot: change `w-2 h-2 rounded-full bg-primary` to `w-9 h-9 rounded-xl bg-primary flex items-center justify-center` with the same SVG icon used in Layout.tsx (person-group icon, white stroke) — consistent branding
    - Assistant name: `text-sm font-bold text-foreground` (bump to bold)
    - Patient name subtitle: `text-xs text-muted-foreground` — keep
    - Right side status indicators:
      - Pending proposals: change from `text-amber-400` to `text-amber-600` (light mode readable)
      - Reports due: change from `text-blue-400` to `text-primary` (use brand colour)
    - Logout: keep `text-muted-foreground hover:text-foreground` — correct

    **Content area:**
    - Keep `max-w-2xl mx-auto px-4 py-6 space-y-5` — correct width for a single-column assistant view

    **Week strip card:**
    - Navigation row: keep prev/next buttons and week label — structure is correct
    - Week hours display (top-right): change from `text-blue-400` to `text-primary` — use brand colour
    - Day grid cells:
      - Today: `bg-primary/10 ring-1 ring-primary/30` (remove /15 opacity — cleaner)
      - Non-today: `bg-white border border-border` (give non-today days a white card surface rather than secondary/30 which looks grey)
      - Zero-hours day: `opacity-40`
      - Day name text: `text-[10px] font-medium uppercase text-muted-foreground` (today: `text-primary`)
      - Day number: `text-sm font-bold` (today: `text-primary`, else: `text-foreground`)
      - Hours text: change from `text-emerald-400` to `text-emerald-600` (light mode readable)
      - Pending dot: change from `bg-amber-400` to `bg-amber-500` (slightly more saturated for visibility)

    **Action banners (proposals / reports due):**
    - REMOVE the dark-mode colours entirely. Replace with light-mode equivalents:
    - Proposals banner:
      `border border-amber-200 bg-amber-50 rounded-xl p-3.5 hover:bg-amber-100 transition-colors text-left`
      - Count badge: `font-mono text-sm font-bold text-amber-700 bg-amber-100 border border-amber-200 rounded px-1.5`
      - Title: `text-sm font-semibold text-amber-800`
      - Subtitle: `text-xs text-amber-600`
    - Reports banner:
      `border border-primary/20 bg-primary/5 rounded-xl p-3.5 hover:bg-primary/10 transition-colors text-left`
      - Count badge: `font-mono text-sm font-bold text-primary bg-primary/10 border border-primary/20 rounded px-1.5`
      - Title: `text-sm font-semibold text-primary`
      - Subtitle: `text-xs text-muted-foreground`

    **Tabs:**
    - Keep `<Tabs value={tab} onValueChange={setTab}>`
    - TabsList: `w-full` — keep
    - Each TabsTrigger: `flex-1` — keep
    - Proposals count badge inside trigger: change from `bg-amber-900 text-amber-400` to
      `bg-amber-100 text-amber-700` (light mode)

    **Shift cards (Upcoming, Proposals, Reports, Open Slots tabs):**
    All tabs follow the same pattern — apply consistent light-mode card styling:

    Upcoming tab cards:
    - Today card ring: `ring-1 ring-primary/30` — keep (correct)
    - Date text: `text-sm font-semibold text-foreground` (bump from medium to semibold)
    - Time range: `font-mono text-lg font-bold text-foreground` — keep
    - Today badge: keep `variant="info"`
    - Status badge: keep `variant="warning"` / `variant="success"` — these are correct

    Proposal cards:
    - Activity description pill: keep inline style `background: act.color + "18", color: act.color` — correct
    - Accept/Decline buttons: keep `variant="approve"` / `variant="reject"` — correct

    Reports tab:
    - Introductory text: `text-xs text-muted-foreground` — keep
    - Each card: keep structure, ensure `rep_status` badge variants are correct
    - Submit button: keep `variant="outline" size="sm"` — correct

    Open slots tab:
    - FillBar component: keep — correct usage
    - Book shift button: keep `variant="outline" size="sm"` — correct

    After editing, run: `cd client && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/rosekaron/Desktop/Kalinga/assistansportal/client && npx tsc --noEmit 2>&1 | tail -10</automated>
    TypeScript must pass. Visually inspect at http://localhost:5173 when logged in as an assistant:
    - No dark-mode colours remain (no bg-amber-950, no border-amber-900/50, no text-blue-300, etc.)
    - Week strip shows a clear light-mode today highlight
    - Action banners use light amber and brand-primary tints
    - All tab content is readable in the light theme
  </verify>
  <done>
    TypeScript passes. All dark-mode colour classes replaced with light-mode equivalents.
    Week hours display uses `text-primary`. Action banners are amber-50 / primary/5 based.
    Tab count badge is amber-100/amber-700. Build exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Assistant dashboard redesigned: header now shows a proper logo icon (matching guardian sidebar),
    week strip uses light colours, action banners use amber-50 and primary tints, tabs are clean,
    all shift cards are consistently styled in the light theme. Dark-mode colour classes eliminated.
  </what-built>
  <how-to-verify>
    1. Log in as an assistant (or use test credentials) and visit the assistant dashboard.
    2. Header: does the logo icon match the guardian sidebar? Is the assistant name clear?
    3. Week strip: does today's date cell have a clear, light-mode highlight? Are hours in green-600?
    4. Action banners: are the proposals and reports-due banners readable on the light background?
       (They should NOT look dark/moody — they should be amber-tinted and brand-blue-tinted respectively.)
    5. Tabs: switch between Upcoming, Proposals, Reports, Open slots — does each tab render correctly?
    6. If there are proposals, accept or reject one — confirm mutations still work.
    7. Overall: does the assistant view now match the professional light-theme established in the
       guardian layout and dashboard?
  </how-to-verify>
  <resume-signal>
    Type "approved" to proceed to Plan 05 (remaining guardian pages).
    Or describe any specific issues and we will iterate.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | assistantSelfApi mutations (accept, reject, submitReport, selfBook) — no changes to mutation logic, only JSX changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.5-04-01 | Tampering | AssistantDashboard.tsx | accept | Pure class string changes; mutation and auth logic unchanged; no new attack surface |
| T-2.5-04-02 | Information Disclosure | AssistantDashboard.tsx | accept | Assistant name and patient name already rendered in header before this plan; no new disclosure |
</threat_model>

<verification>
- `npx tsc --noEmit` passes in `client/`
- `npm run build` exits 0 in `client/`
- No dark-mode colour classes (bg-amber-950, border-amber-900, text-blue-300, etc.) remain
- Week strip uses light-mode palette
- Action banners use amber-50 and primary/5 backgrounds
- Tab count badge uses amber-100/amber-700
- Human approves at checkpoint
</verification>

<success_criteria>
Assistant dashboard is visually polished and consistent with the guardian light theme. All dark-mode
artefacts eliminated. Shift information is clearly communicated. Build and TypeScript clean.
Human approves at checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-ui-overhaul/02.5-04-SUMMARY.md` summarising:
- Key class changes: what dark-mode classes were replaced and with what
- Any design decisions made during implementation (e.g. icon choice for header)
- Human checkpoint feedback and any iterations
- Build and TypeScript status
</output>
