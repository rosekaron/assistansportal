---
created: 2026-04-25T00:00:00Z
title: v1.0.2 hardening — outstanding code-review findings from PR #7
area: backend / security / data-integrity
source: .planning/phases/10-real-data-entry/10-REVIEWS.md (CodeRabbit review of milestone/v1.0.1 → main, run 2026-04-25)
planned_milestone: v1.0.2 (proposed)
status: pending
priority: high (security + data-integrity items present)
---

# v1.0.2 hardening — outstanding code-review findings

CodeRabbit reviewed the full v1.0.1 PR (#7, merged 2026-04-25) and surfaced 23 findings across the entire shipping branch. v1.0.1 was merged with these documented as deferred — the milestone's actual scope (salary slip + foundation cleanup + employer helper + real-data E2E) was verified clean on its own merits. **Most HIGH-severity items below are pre-existing latent issues (predate v1.0.1), not regressions introduced by the milestone.**

Full per-finding evidence: `.planning/phases/10-real-data-entry/10-REVIEWS.md`.

## HIGH — security + data integrity (fix in v1.0.2)

| # | File | Issue | Suggested fix |
|---|------|-------|---------------|
| 1 | `server/src/routes/clock.ts:52-69` | TOCTOU on clock-in: concurrent requests can create duplicate unverified entries | Unique constraint on `(assistantId, date, startTime)` + Drizzle migration, OR wrap in a `SERIALIZABLE` transaction |
| 2 | `server/src/routes/clock.ts:147-165` | Multi-table DB ops without transaction | Wrap in `db.transaction(...)` |
| 3 | `server/src/routes/assistants.ts:89-145` | Same multi-table-ops-without-transaction concern | Wrap in transaction |
| 4 | `server/src/routes/auth.ts:229-234` | Missing UNIQUE constraint on `assistantGuardianLinks(assistantId, guardianId)` — duplicate links possible | Add Drizzle `uniqueIndex` migration |
| 5 | `server/src/routes/gcal.ts:20-38` | JWT in query parameter is logged in server access logs | Move to header or signed short-lived URL pattern |
| 6 | `server/src/routes/entries.ts:45-48` | `e.message` returned to clients on 500 — info disclosure (constraint names, table structure) | Log full error server-side, return generic to client |

## MEDIUM — track but not blocking

7. `server/src/middleware/auth.ts:43-46` — Error message text doesn't match the actual check.
8. `server/src/routes/assistant.ts:73-86` + `:10` — Absence query lacks guardian scoping. **Documented v1 single-tenant assumption** per Phase 9 RESEARCH.md Pitfall 6 — accept as-is until multi-tenant milestone.
9. `client/src/pages/Records.tsx:210-218` — Absence year filter misses entries spanning year boundaries (e.g. 2025-12-30 → 2026-01-05).
10. `client/src/pages/Monthly.tsx:594-599` — Step 2 state never shows "locked" — minor UX inconsistency.
11. `server/src/lib/pdfSlipRenderer.ts:153` — `semesterYtdUsed` field referenced but not populated; "tagna i år" displays incorrectly.
12. `client/src/pages/Home.tsx:253-258` — Nested interactive elements (a11y / event-bubbling concern).
13. `server/src/routes/__tests__/pdf-lonespec.test.ts:270-280` — Test fails if `JWT_SECRET` env var is not set in CI.
14. `client/e2e/gcal.spec.ts:243-266` — Test assertions may not match actual UI behavior.

## LOW — docs hygiene (one-line fixes, batch them)

15. `.planning/phases/07-foundation-schema-cleanup/07-03-PLAN.md:134-135` — duplicate acceptance criterion.
16. `.planning/milestones/v1.0-ROADMAP.md:648-652` — inconsistent Phase 6 completion status.
17. `.planning/phases/09-salary-slip/09-02-slip-builder-and-renderer-PLAN.md:487-495` — malformed markdown table row.
18. `.planning/phases/08-employer-representation-helper/08-VERIFICATION.md:40-42` + `:108-113` — markdown table column-count mismatches (×2).
19. `.planning/todos/pending/2026-04-18-home-notification-for-labor-law-and-fk-schedule-violations.md` — clarify error-blocking vs override logic; clarify milestone version numbering.

## EXTERNAL VALIDATION of an already-known gap

20. `hourlyRateOverride = 0.31` Settings parsing bug — flagged by CodeRabbit (validates the gap we already logged in `10-03-SUMMARY.md`'s Gaps section). Likely Swedish decimal-comma parsing issue in the Settings input field. Track as a separate UI bug, not part of this hardening sweep.

## How to pick this up

For the next session / new LLM:

1. Read `.planning/phases/10-real-data-entry/10-REVIEWS.md` first — it has the verbatim CodeRabbit output with `Prompt for AI Agent` sections per finding.
2. Decide milestone scope: just HIGH 1-6 (focused security/integrity sprint, ~1 day), or HIGH + MEDIUM (~2 days), or HIGH + MEDIUM + LOW (~2.5 days).
3. Run `/gsd-new-milestone v1.0.2` (or absorb into the next planned milestone if there's already one queued).
4. The hourly-rate parsing bug (item 20) is a separate UI investigation — file as its own bug or incorporate into the Settings polish work.

## What MUST NOT be lost

- Items 1-6 each have an actual `Prompt for AI Agent` block in `10-REVIEWS.md` with the specific change instructions. Use those as the starting point — CodeRabbit already wrote the executable prompt.
- The pre-existing nature of items 1-6 is real: they didn't appear in v1.0.1, they were just first surfaced when CodeRabbit reviewed the cumulative diff. Don't blame the milestone for them.
