---
phase: 02.5-ui-overhaul
plan: 03
type: execute
wave: 3
depends_on:
  - 02.5-02-PLAN.md
files_modified:
  - client/src/components/Layout.tsx
  - client/src/pages/Dashboard.tsx
  - client/src/components/shared.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Guardian sidebar looks polished and enterprise-appropriate — not a prototype"
    - "Logo treatment has clear product identity — icon + name are readable and professional"
    - "Nav active state is visually unambiguous — the selected page is immediately obvious"
    - "Weekly hours widget in sidebar is visually prominent — key status information at a glance"
    - "Guardian Dashboard card grid has professional spacing, shadows, and hierarchy"
    - "The hours fill bar on the Dashboard communicates schedule coverage clearly"
    - "Needs Attention and FK Invoice cards are readable and well-structured"
    - "Typography hierarchy is clear: page titles, section labels, data values have distinct weights"
  artifacts:
    - path: "client/src/components/Layout.tsx"
      provides: "Redesigned guardian sidebar with polished nav, logo, and weekly hours widget"
      min_lines: 90
    - path: "client/src/pages/Dashboard.tsx"
      provides: "Redesigned guardian dashboard with improved card layout and visual hierarchy"
      min_lines: 250
    - path: "client/src/components/shared.tsx"
      provides: "Optionally updated PageHeader spacing and SectionLabel style if needed"
  key_links:
    - from: "client/src/components/Layout.tsx"
      to: "client/src/pages/Dashboard.tsx"
      via: "React Router <Outlet /> rendering inside Layout's <main>"
      pattern: "<Outlet"
---

<objective>
Redesign the guardian layout shell (sidebar, nav, logo, weekly hours widget) and the Guardian
Dashboard page (card grid, fill bar, action cards, assistant quick-view) to B2B-professional standard.

Purpose: These are the first and primary views a guardian and a B2B evaluator (Humana, Attendo) see.
Layout.tsx wraps every guardian page — improving it lifts all pages simultaneously. Dashboard.tsx is
the home view and sets the tone for the entire product. Per D-04 and D-06.

Output: Polished Layout.tsx and Dashboard.tsx. Routes unchanged. Data fetching unchanged. Visual layer only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.5-ui-overhaul/02.5-RESEARCH.md
@.planning/phases/02.5-ui-overhaul/02.5-CONTEXT.md
@.planning/phases/02.5-ui-overhaul/02.5-02-SUMMARY.md
@client/src/components/Layout.tsx
@client/src/pages/Dashboard.tsx
@client/src/components/shared.tsx
</context>

<interfaces>
<!-- Contracts the executor MUST preserve — do not change these interfaces -->

From client/src/components/Layout.tsx (existing — keep all imports, store usage, and data fetching):
```tsx
// These must not change:
const logout   = useAuthStore((s) => s.logout);
const navigate = useNavigate();
const { data: profile } = useQuery({ queryKey: ["profile"], queryFn: () => profileApi.get()... });
// Nav items array structure — keep all 6 nav items and their routes
const nav = [
  { to: "/dashboard", label: "Dashboard", icon: LayoutDashboard },
  { to: "/calendar",  label: "Schedule",  icon: CalendarDays },
  { to: "/leave",     label: "Frånvaro",  icon: CalendarOff },
  { to: "/reports",   label: "Reports",   icon: FileText },
  { to: "/assistants",label: "Assistants",icon: Users },
  { to: "/settings",  label: "Settings",  icon: Settings },
];
```

From client/src/pages/Dashboard.tsx (existing — keep all data logic intact):
```tsx
// These calculations must not change — only JSX/class names change:
const scheduledHours, byDay, pendingApprovals, invoiceHours, invoiceTotalHrs
// All React Query hooks: profile, assistants, entries
// All navigation: navigate("/calendar"), navigate("/reports"), navigate("/assistants")
// fkDownload mutation — keep exactly
```

From client/src/components/shared.tsx:
```tsx
// Keep existing exports — only adjust className strings if needed:
export function PageHeader({ title, description, action })
export function AssistantAvatar({ name, initials, color, size })
export function SectionLabel({ children })
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign guardian Layout.tsx — sidebar, logo, nav active states, weekly hours widget</name>
  <files>client/src/components/Layout.tsx</files>
  <action>
    Rewrite ONLY the JSX and className strings in Layout.tsx. All imports, hooks, data fetching,
    event handlers, and the nav array remain unchanged.

    Design targets (D-04, D-06, care.com professional aesthetic):

    **Sidebar overall:**
    - Keep `w-60 shrink-0 flex flex-col` — width is correct
    - Use `bg-white border-r border-border` for the sidebar surface — clean, professional
    - Add `shadow-sm` on the aside for subtle depth separation

    **Logo area (top of sidebar):**
    - Icon: keep the SVG person-group icon, keep `bg-primary` pill — looks good
    - Product name: make "Assistansportal" `text-sm font-bold text-foreground` (currently font-semibold — bump to bold)
    - "For {patientName}" subtitle: keep `text-xs text-muted-foreground` — correct
    - Add a thin horizontal rule or additional bottom padding after the logo section to separate it
      from nav more clearly. Use `border-b border-border pb-5 mb-2` on the logo div.
    - Increase logo icon size from 32×32 to 36×36 (`w-9 h-9`) for slightly more presence.

    **Nav links:**
    - Keep the `rounded-xl px-3 py-2.5` pill shape — this is correct
    - Active state: `bg-primary text-primary-foreground shadow-sm` — solid, clear
    - Inactive state: `text-muted-foreground hover:bg-muted hover:text-foreground` — use `bg-muted`
      instead of `bg-secondary` for subtler hover (muted is lighter than secondary in the updated tokens)
    - Icon size: keep `style={{ width: 18, height: 18 }}` inline override — correct
    - Add `transition-colors duration-150` explicitly for smooth hover

    **Weekly hours widget (sidebar bottom):**
    - This is key status information. Promote it visually.
    - Change from `bg-blue-50` to `bg-primary/8` (or `bg-primary/10`) — ties it to the brand colour
    - Container: `rounded-xl px-4 py-3` (slightly more padding than current `px-3 py-2.5`)
    - Label: `text-xs font-medium text-muted-foreground uppercase tracking-wide` (add uppercase tracking)
    - Value: `text-2xl font-bold text-primary` (bump from text-lg to text-2xl — this is the key number)
    - "/week" suffix: keep `text-xs font-normal text-muted-foreground`
    - Add `border border-primary/15` to the widget container for subtle definition

    **Logout button:**
    - Keep existing `hover:bg-red-50 hover:text-red-600` — correct destructive pattern
    - Make sure `text-muted-foreground` base state is correct (it is)

    **Main content area:**
    - Keep `flex-1 overflow-y-auto bg-background`
    - Keep `max-w-5xl mx-auto px-8 py-8` — correct content width

    After editing, run: `cd client && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/rosekaron/Desktop/Kalinga/assistansportal/client && npx tsc --noEmit 2>&1 | tail -10</automated>
    TypeScript must pass. Visually inspect at http://localhost:5173/dashboard: sidebar should look
    polished with a clearly readable active nav state and a prominent weekly hours widget.
  </verify>
  <done>
    TypeScript passes. Sidebar renders with: professional white background, prominent nav active state,
    larger logo, and a clearly readable weekly hours widget with brand-coloured background.
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign Dashboard.tsx — card hierarchy, fill bar, action cards</name>
  <files>client/src/pages/Dashboard.tsx</files>
  <action>
    Rewrite ONLY the JSX and className strings in Dashboard.tsx. All data logic, React Query hooks,
    mutations, calculated values (byDay, scheduledHours, weekHours, pendingApprovals, etc.), and
    navigation handlers remain unchanged.

    Design targets (D-04, D-06, care.com professional aesthetic):

    **Setup banner (line 97–109):**
    - Keep blue-50 / blue-200 / blue-800 colour scheme — this is correct for an informational banner
    - Increase border radius of button from `rounded-lg` to `rounded-xl` for consistency
    - Add `font-medium` to the button text if not already present

    **PageHeader:**
    - No change needed — PageHeader component handles title/description correctly

    **Week coverage card:**
    - Card title "This week's coverage": bump from `text-sm font-semibold` to `text-base font-semibold`
    - Day grid cells: keep the current structure but improve the today highlight:
      - Today cell: `border-primary/40 bg-primary/8 ring-1 ring-primary/20` (more visible today state)
      - Non-today, empty: keep `opacity-50`
      - Non-today, has entries: `border-border bg-secondary/20 hover:bg-secondary/40 transition-colors`
    - Day name text: keep `text-[10px] font-semibold uppercase tracking-wide` — correct
    - Hours fill bar:
      - Track: `h-2.5 bg-muted rounded-full` (slightly taller bar — more visible)
      - Fill: keep `bg-blue-500` (intentional semantic blue, not primary — correct per RESEARCH.md)
      - Labels below bar: keep as-is

    **Action row (2-column grid):**

    "Needs attention" card:
    - Section label: keep `text-xs font-semibold uppercase tracking-wide text-muted-foreground`
    - All-clear state: keep `text-emerald-600` with CheckCircle2 — correct
    - Pending approvals button: keep amber-50 / amber-200 styling — this is correct semantic colour
    - FK due soon alert: keep red-50 / amber-50 styling — correct

    "FK invoice" card:
    - Invoice hours value: change from `text-blue-400` to `text-primary` — use the token, not a
      hardcoded colour. `text-3xl font-mono font-bold text-primary`
    - Badge: keep existing variant=success/warning pattern
    - Progress bar fill: keep `bg-emerald-500` (semantic: approved = green — correct)
    - Download button: keep `variant="outline"` — appropriate for secondary action

    **Assistants quick-view card:**
    - Section label: keep as-is
    - Each assistant entry: add `hover:bg-muted/50 rounded-lg px-2 py-1.5 transition-colors -mx-2`
      for subtle hover affordance on the row
    - Avatar + name layout: keep existing structure

    After editing, run: `cd client && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/rosekaron/Desktop/Kalinga/assistansportal/client && npx tsc --noEmit 2>&1 | tail -10</automated>
    TypeScript must pass. Visually inspect at http://localhost:5173/dashboard: cards should look
    structured, the week grid should have a clear today indicator, and the invoice hours should
    use primary colour instead of the previous blue-400.
  </verify>
  <done>
    TypeScript passes. Dashboard renders with clear card hierarchy, professional week coverage grid,
    and FK invoice section using `text-primary` instead of hardcoded `text-blue-400`.
    All data and navigation still works (no functional regressions).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Guardian Layout sidebar and Dashboard page redesigned to B2B professional standard per D-04.
    Sidebar has a polished logo treatment, clear nav active states, and a prominent weekly hours
    widget. Dashboard has improved card hierarchy and typography.
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:5173/dashboard
    2. Guardian layout sidebar:
       - Logo: is the icon+name clearly readable and professional?
       - Nav: is the active page immediately obvious (clear active state)?
       - Weekly hours widget: does it stand out as important status information?
       - Overall: would this sidebar look credible in a B2B demo to Humana or Attendo?
    3. Dashboard main area:
       - Week coverage grid: is today's date clearly highlighted?
       - Action cards: are the "Needs attention" and "FK invoice" cards readable and well-structured?
       - FK invoice hours: does it now use the brand primary colour (deep blue, not the previous light blue-400)?
    4. Click each nav item (Schedule, Frånvaro, Reports, Assistants, Settings) — verify nav still works
       and active state updates correctly.
    5. Log out and back in — verify the hours widget shows data correctly.
  </how-to-verify>
  <resume-signal>
    Type "approved" to proceed to Plan 04 (Assistant Dashboard redesign).
    Or describe any specific improvements needed and we will iterate.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Guardian profile data (patientName, weeklyHours) flows from API into sidebar — no changes to how it is fetched or displayed; existing React Query caching and error handling preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.5-03-01 | Information Disclosure | Layout.tsx | accept | patientName rendered in sidebar — same as before this plan; no new disclosure surface introduced |
| T-2.5-03-02 | Tampering | Dashboard.tsx | accept | Pure class string changes; no data flow, auth, or mutation logic changes |
</threat_model>

<verification>
- `npx tsc --noEmit` passes in `client/`
- `npm run build` exits 0 in `client/`
- Sidebar nav active state is visually unambiguous
- Weekly hours widget is prominent and uses brand colour
- Dashboard FK invoice hours use `text-primary` (not `text-blue-400`)
- All guardian routes still navigate correctly
- Human approves the visual result
</verification>

<success_criteria>
Guardian layout shell and Dashboard look B2B-professional. Sidebar is polished and enterprise-appropriate.
Dashboard card hierarchy is clear. TypeScript and build are clean. Human approves at checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-ui-overhaul/02.5-03-SUMMARY.md` summarising:
- Key class changes made in Layout.tsx and Dashboard.tsx
- Any deviations from the planned design direction (and why)
- Human checkpoint feedback and any iterations made
- Build and TypeScript status
</output>
