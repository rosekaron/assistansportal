---
phase: 02.5-ui-overhaul
plan: 05
type: execute
wave: 4
depends_on:
  - 02.5-03-PLAN.md
  - 02.5-04-PLAN.md
files_modified:
  - client/src/pages/Leave.tsx
  - client/src/pages/Login.tsx
  - client/src/pages/Calendar.tsx
  - client/src/pages/Reports.tsx
  - client/src/pages/Assistants.tsx
  - client/src/pages/Settings.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Leave page balance cards are visually consistent with Dashboard card style"
    - "Leave table rows are well-spaced and easy to scan"
    - "Login page is the first impression for a B2B evaluator — it must look professional and trustworthy"
    - "Calendar, Reports, Assistants, and Settings pages use the same typographic hierarchy and card patterns as Dashboard"
    - "No page has leftover dark-mode or inconsistent colour values after the consistency pass"
    - "All pages pass TypeScript check and build without errors"
  artifacts:
    - path: "client/src/pages/Leave.tsx"
      provides: "Polished absence management page with consistent card and table styling"
    - path: "client/src/pages/Login.tsx"
      provides: "Professional login page appropriate for B2B first impression"
    - path: "client/src/pages/Calendar.tsx"
      provides: "Calendar page with consistent card headers and layout"
    - path: "client/src/pages/Reports.tsx"
      provides: "Reports page with consistent table and action styling"
    - path: "client/src/pages/Assistants.tsx"
      provides: "Assistants list page with consistent card and avatar usage"
    - path: "client/src/pages/Settings.tsx"
      provides: "Settings page with consistent form control styling"
  key_links:
    - from: "client/src/pages/Login.tsx"
      to: "client/src/store/auth"
      via: "useAuthStore login mutation"
      pattern: "useAuthStore"
---

<objective>
Apply the design system established in Plans 02–04 consistently across all remaining guardian pages
(Leave, Calendar, Reports, Assistants, Settings) and the Login page.

Purpose: Plans 03 and 04 established the design language. Plan 05 carries it through every remaining
view so the product is visually consistent end-to-end. Login is included because it is the first
screen a B2B evaluator sees — per D-03. All remaining guardian pages are in scope per RESEARCH.md audit.

Output: Six pages updated. All pages pass TypeScript check and build. Human approves visual pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.5-ui-overhaul/02.5-RESEARCH.md
@.planning/phases/02.5-ui-overhaul/02.5-CONTEXT.md
@.planning/phases/02.5-ui-overhaul/02.5-03-SUMMARY.md
@.planning/phases/02.5-ui-overhaul/02.5-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consistency pass — Leave.tsx and Login.tsx</name>
  <files>client/src/pages/Leave.tsx, client/src/pages/Login.tsx</files>
  <action>
    Read each file in full before editing. Then apply the design system consistently.
    All data fetching, mutations, and business logic remain unchanged — only JSX and className strings.

    **Leave.tsx targets:**
    - Balance cards (VAB balance, sick leave): use the same card pattern as Dashboard — `Card > CardContent` with `pt-5`
    - Balance values: `text-2xl font-bold text-foreground` (large, readable number)
    - Balance labels: `text-xs font-medium text-muted-foreground uppercase tracking-wide`
    - Status colours: keep existing amber/emerald/red semantic colours — these are correct
    - Filter row (type selector, date range): ensure inputs use `border-input` and `rounded-xl` radius
    - Absence table:
      - Table header row: `text-xs font-semibold uppercase tracking-wide text-muted-foreground`
      - Table rows: `border-b border-border` with `py-3` vertical padding for comfortable density
      - Status badge: keep existing `variant="success"` / `variant="warning"` / `variant="destructive"` — correct
    - Empty state: use the `EmptyState` component from shared.tsx if not already used
    - Add absence button: `Button` with `variant="default"` (filled primary) — this is the primary action

    **Login.tsx targets:**
    This is the B2B first impression. It must look professional and trustworthy.

    - Outer layout: `min-h-screen bg-muted/30 flex items-center justify-center`
    - Card: a centered `Card` with max-width `max-w-sm w-full` and explicit `shadow-lg`
    - Logo/header area inside the card:
      - Same logo icon as Layout.tsx (w-10 h-10 rounded-xl bg-primary, SVG person-group icon in white)
      - Product name: `text-xl font-bold text-foreground`
      - Subtitle: `text-sm text-muted-foreground text-center` — e.g. "Assistansportal · Hantera din assistans"
    - Form fields: use `label + input` pattern with `text-sm font-medium text-foreground` labels
      and `rounded-xl border-input` inputs — match the existing input component styling
    - Login button: `Button variant="default" className="w-full"` — full-width primary action
    - Error message: `text-sm text-destructive` centered below the form
    - Do NOT add a "Register" link — this is an invite-only platform
    - Do NOT add Google OAuth button (if not already present) — keep existing auth flow only

    After editing both files, run: `cd client && npx tsc --noEmit 2>&1 | tail -10`
  </action>
  <verify>
    <automated>cd /Users/rosekaron/Desktop/Kalinga/assistansportal/client && npx tsc --noEmit 2>&1 | tail -10</automated>
    TypeScript must pass. Visually confirm:
    - Leave page: balance cards match Dashboard card style
    - Login page: looks professional — card-based, centered, with logo, no jarring contrast
  </verify>
  <done>
    TypeScript passes. Leave.tsx and Login.tsx updated. Leave balance cards consistent with Dashboard.
    Login page is professional, card-centered, with matching logo treatment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Consistency pass — Calendar.tsx, Reports.tsx, Assistants.tsx, Settings.tsx</name>
  <files>
    client/src/pages/Calendar.tsx,
    client/src/pages/Reports.tsx,
    client/src/pages/Assistants.tsx,
    client/src/pages/Settings.tsx
  </files>
  <action>
    Read each file in full before editing. Apply the design system consistently across all four pages.
    The goal is NOT a full redesign — it is a consistency pass to eliminate any leftover colour
    inconsistencies, tighten spacing, and ensure card/table patterns match Dashboard.

    For each page, apply these principles:

    **Consistent elements to check and fix in every page:**
    1. Page title / PageHeader: ensure `PageHeader` component is used (not ad-hoc h1 tags)
    2. Section labels: `text-xs font-semibold uppercase tracking-wide text-muted-foreground mb-3`
    3. Card containers: use `Card > CardContent className="pt-5"` pattern
    4. Table header rows: `text-xs font-semibold uppercase tracking-wide text-muted-foreground`
    5. Table rows: `border-b border-border py-3` density
    6. Status badges: use existing Badge variants — do not invent new colour patterns
    7. Hardcoded dark colours: replace any `text-blue-400`, `text-amber-400`, `bg-blue-950`, etc.
       with light-mode equivalents (text-primary, text-amber-600, bg-amber-50, etc.)
    8. Buttons: primary actions use `variant="default"`, secondary/link-like actions use `variant="outline"`
       or `variant="ghost"`
    9. Empty states: use `EmptyState` from shared.tsx

    **Calendar.tsx specific:**
    - This is likely a week/month grid — ensure the today highlight uses `bg-primary/10 border-primary/30`
    - Day headers: `text-xs font-semibold uppercase tracking-wide text-muted-foreground`
    - Entry pills: ActivityPill component if used, otherwise consistent colour coding

    **Reports.tsx specific:**
    - Report list rows should have clear typographic hierarchy: date, assistant name, hours, status
    - Approve/reject buttons: keep existing `variant="approve"` / `variant="reject"` — correct
    - Filter controls (if present): ensure `rounded-xl border-input` inputs
    - Download FK button: `variant="outline"` with FileDown icon — consistent with Dashboard

    **Assistants.tsx specific:**
    - Assistant cards/rows: use AssistantAvatar component consistently
    - Invite button: `variant="default"` (primary action)
    - Table or card list: ensure `border-b border-border` row separators

    **Settings.tsx specific:**
    - Section groupings: use Card containers with section headers
    - Form labels: `text-sm font-medium text-foreground`
    - Inputs: `rounded-xl border-input` — consistent
    - Save button: `variant="default"` (primary action)
    - Destructive settings (e.g. delete account): use `variant="destructive"` button

    After editing all four files, run: `cd client && npx tsc --noEmit 2>&1 | tail -10`
    Then run: `cd client && npm run build 2>&1 | tail -5`
  </action>
  <verify>
    <automated>cd /Users/rosekaron/Desktop/Kalinga/assistansportal/client && npx tsc --noEmit 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
    TypeScript must pass. Build must exit 0. Do a visual spot-check of each page:
    - No dark-mode artefacts remaining
    - Consistent card, table, badge, and button patterns
  </verify>
  <done>
    TypeScript and build both pass. All four pages use consistent card/table/badge patterns.
    No dark-mode colour classes remain. All pages pass the visual consistency check.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full visual consistency pass across all guardian pages and the Login page. The entire product
    now uses the same design language established in Plans 02–04: care.com-inspired professional
    B2B healthcare aesthetic, light theme throughout, consistent typography and component patterns.
  </what-built>
  <how-to-verify>
    Navigate through every page of the guardian portal and confirm visual consistency:
    1. Login (http://localhost:5173/login) — professional card-centered login, logo present
    2. Dashboard (http://localhost:5173/dashboard) — already approved in Plan 03
    3. Leave / Frånvaro (http://localhost:5173/leave) — balance cards, absence table
    4. Reports (http://localhost:5173/reports) — report rows, approve/reject buttons
    5. Schedule / Calendar (http://localhost:5173/calendar) — calendar grid with today highlight
    6. Assistants (http://localhost:5173/assistants) — assistant cards with avatars
    7. Settings (http://localhost:5173/settings) — form controls, section groupings

    For each page, verify:
    - No dark-mode colour leftovers (no amber-400, blue-400 in light contexts, no bg-*-950)
    - Card surfaces are white on off-white page background
    - Typography hierarchy is clear (page title > section label > body > muted)
    - Primary actions use filled buttons, secondary actions use outline/ghost

    Final gate: does the overall product feel ready for a B2B demo to Humana or Attendo?
    Would you be comfortable showing this to a potential enterprise customer?
  </how-to-verify>
  <resume-signal>
    Type "approved — phase 2.5 complete" if the product is visually ready for Phase 3.
    Or list the specific pages or elements that still need adjustment.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Login form → auth API | Login.tsx submits credentials — no changes to auth mutation or token storage, only layout JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.5-05-01 | Tampering | Login.tsx | accept | Auth flow (useAuthStore, API call) unchanged; only outer layout JSX changed; no new input fields added |
| T-2.5-05-02 | Information Disclosure | Login.tsx | accept | No new error messages added that could leak info; existing error handling preserved |
</threat_model>

<verification>
- `npx tsc --noEmit` passes in `client/`
- `npm run build` exits 0 in `client/`
- All 6 pages updated and visually consistent
- Login page looks professional — card-centered, logo visible
- No dark-mode artefacts on any guardian page
- Human approves full visual consistency pass at checkpoint
</verification>

<success_criteria>
All guardian pages and the Login page are visually consistent and B2B-appropriate. The design system
established across Phase 2.5 is complete. Phase 3 (Payroll Calculation) can begin without visual debt.
Build and TypeScript are clean. Human signs off with "approved — phase 2.5 complete".
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-ui-overhaul/02.5-05-SUMMARY.md` summarising:
- Pages updated and key changes per page
- Any pages where no changes were needed (already consistent)
- Any deviations from the planned approach and why
- Final human checkpoint feedback
- Build and TypeScript status

Also update `.planning/phases/02.5-ui-overhaul/02.5-VALIDATION.md`:
- Set `nyquist_compliant: true`
- Set `wave_0_complete: true`
- Update the verification map task IDs with actual plan/task references from this phase
</output>
