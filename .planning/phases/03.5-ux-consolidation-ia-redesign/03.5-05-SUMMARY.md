---
phase: 03.5-ux-consolidation-ia-redesign
plan: "05"
subsystem: ui
tags: [react, tanstack-query, radix-ui, lucide-react, tailwind, schedule-grid, absence]

# Dependency graph
requires:
  - phase: 03.5-04
    provides: App.tsx with /home stub route + Layout.tsx 4-item nav

provides:
  - Home.tsx page — 7-day schedule grid, GCal status, pending actions, mark-absent dialog
  - /home route wired to HomePage (not Dashboard stub)

affects: [03.5-06, 03.5-07, 03.5-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "FK download gate: disabled when invoicePending > 0 OR invoiceHours === 0"
    - "Mark absent dialog uses Radix Dialog + Select from custom component library"
    - "payrollPendingCount derived from payrollApi.list(invoiceMonthStr) filtered to status=draft"

key-files:
  created:
    - client/src/pages/Home.tsx
  modified:
    - client/src/App.tsx

key-decisions:
  - "FK gate uses both invoicePending and invoiceHours=0 checks — disables download until all reports approved AND hours exist"
  - "Mark absent quick-action shows first assistant for the day; guardian selects absence type via Select component"
  - "GCal banner shown inline above schedule (not a dismissable toast) — persistent nudge to connect"
  - "Dashboard import kept in App.tsx since /monthly and /records still use it as stubs (Plans 06+07)"

patterns-established:
  - "Home page pattern: data-only queries (no mutation side effects) for schedule + FK state; mutations only for mark-absent and FK download"

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-04-11
---

# Phase 03.5 Plan 05: Home Page Summary

**Guardian home page with 7-day schedule grid, GCal status banner, FK-gated download, payroll pending count, and mark-absent dialog using Radix components**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-11T00:00:00Z
- **Completed:** 2026-04-11T00:15:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Created Home.tsx (469 lines) with all 5 sections from the UI-SPEC layout contract
- FK download button gated correctly: disabled when `invoicePending > 0` OR `invoiceHours === 0`
- Payroll pending count displayed using `payrollApi.list(invoiceMonthStr)` filtered to `status === "draft"`
- Mark absent dialog creates absence record via `absenceApi.create()` and invalidates entries + absences queries on success
- /home route in App.tsx replaced from Dashboard stub to real HomePage component
- TypeScript compiles cleanly — zero errors

## Task Commits

1. **Task 1: Create client/src/pages/Home.tsx** — `f06b23d` (feat)
2. **Task 2: Wire Home.tsx into App.tsx /home route** — `9201135` (feat)

## Files Created/Modified

- `client/src/pages/Home.tsx` — New Home page: setup banner, GCal status, 7-day schedule grid, pending actions + FK section, mark-absent dialog
- `client/src/App.tsx` — Added `import HomePage from "@/pages/Home"`, replaced `/home` stub route

## Decisions Made

- FK gate checks both `invoicePending > 0` AND `invoiceHours === 0` (per threat model T-3.5-15 and plan spec)
- Mark absent quick-action uses the first assistant found for a given day slot — this is reasonable since the guardian sees who is working and selects the correct day
- GCal banner is a persistent inline element (not dismissable) to ensure guardian is always prompted to connect
- Dashboard import kept in App.tsx because Plans 06 and 07 still use it as a stub for `/monthly` and `/records`

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Known Stubs

None — all data sources are wired. The schedule grid uses real `entriesApi` data; pending counts use real `payrollApi` data; mark-absent calls real `absenceApi.create()`.

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary changes introduced. FK download gate (T-3.5-15) implemented as specified: `invoicePending > 0` check applied client-side on the Button `disabled` prop.

## Next Phase Readiness

- Home page complete and wired at `/home`
- Plan 06 (Monthly.tsx) can now replace the Dashboard stub at `/monthly`
- Plan 07 (Records.tsx) can then replace the Dashboard stub at `/records`
- After Plans 06+07, the Dashboard import in App.tsx can be safely removed

---
*Phase: 03.5-ux-consolidation-ia-redesign*
*Completed: 2026-04-11*

## Self-Check: PASSED

- FOUND: `client/src/pages/Home.tsx`
- FOUND: `.planning/phases/03.5-ux-consolidation-ia-redesign/03.5-05-SUMMARY.md`
- FOUND: commit `f06b23d` (Task 1)
- FOUND: commit `9201135` (Task 2)
