---
phase: 02.5-ui-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 02.5-01-PLAN.md
files_modified:
  - client/src/index.css
  - client/tailwind.config.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Primary colour conveys professional trust — deeper navy/blue appropriate for B2B healthcare, not a washed-out mid-blue"
    - "Background and card surfaces have the correct warmth — off-white page, clean white cards"
    - "Token changes apply globally to all pages without any component-level edits"
    - "The app still builds without TypeScript errors after token changes"
  artifacts:
    - path: "client/src/index.css"
      provides: "Updated HSL token values in :root block"
      contains: "--primary:"
    - path: "client/tailwind.config.js"
      provides: "Colour palette matched to updated token values"
      contains: "primary:"
  key_links:
    - from: "client/src/index.css"
      to: "client/tailwind.config.js"
      via: "hsl(var(--primary)) pattern in colors extension"
      pattern: "hsl\\(var\\(--primary\\)\\)"
---

<objective>
Update the CSS design token values in `index.css` to achieve a B2B-grade, trust-inspiring colour
palette after visually confirming the real render from Plan 01's Tailwind fix.

Purpose: The token system is already architecturally correct (HSL triplets, `hsl(var(--token))`
references in tailwind.config.js). Plan 02 sets the right HSL values inside those tokens. All
components using `bg-primary`, `text-muted-foreground`, `bg-card`, etc. automatically inherit the
new palette — no component-level edits needed.

Output: Updated `client/src/index.css` token block. Build green, visual palette changed globally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.5-ui-overhaul/02.5-RESEARCH.md
@.planning/phases/02.5-ui-overhaul/02.5-CONTEXT.md
@.planning/phases/02.5-ui-overhaul/02.5-01-SUMMARY.md

<!-- Current token inventory (from client/src/index.css :root — read before editing):
  --background:        0 0% 97%        warm off-white
  --foreground:        220 15% 20%     dark slate
  --card:              0 0% 100%       white
  --card-foreground:   220 15% 20%
  --popover:           0 0% 100%
  --popover-foreground: 220 15% 20%
  --primary:           201 70% 42%     current: calm mid-blue (#1a8fb0 range)
  --primary-foreground: 0 0% 100%
  --secondary:         210 20% 93%     light blue-grey
  --secondary-foreground: 220 15% 35%
  --muted:             210 20% 95%
  --muted-foreground:  220 10% 50%
  --accent:            210 20% 93%
  --accent-foreground: 220 15% 25%
  --destructive:       0 65% 48%
  --destructive-foreground: 0 0% 100%
  --border:            210 20% 88%
  --input:             210 20% 88%
  --ring:              201 70% 42%
  --radius: 0.75rem

  IMPORTANT — do NOT change semantic status colours:
  - bg-blue-50, bg-amber-50, bg-red-50, bg-emerald-500, bg-blue-500 (used for status states in
    Dashboard.tsx and AssistantDashboard.tsx) — these are intentionally hardcoded to Tailwind's
    built-in palette and are NOT controlled by the token system.

  IMPORTANT — do NOT change --radius; it is already well-calibrated.

  Design direction (from CONTEXT.md D-02, D-03):
  - care.com aesthetic: clean, professional, accessible, trust-inspiring healthcare
  - Must not feel like a hobby project
  - Appropriate for a B2B demo to Humana / Attendo (Swedish care organisations)
  - The current primary (201 70% 42%) is acceptable but may benefit from shifting toward a
    deeper, more authoritative navy-leaning blue (e.g. 213 80% 38% or similar) -->
</context>

<interfaces>
<!-- Token → component class mapping — how token changes propagate.
     These classes are already used across the codebase; no component edits needed for palette changes.

From client/src/index.css:
```css
:root {
  --primary:   {H S% L%}   /* consumed as bg-primary, text-primary, border-primary, ring */
  --muted:     {H S% L%}   /* consumed as bg-muted, bg-secondary/30 */
  --border:    {H S% L%}   /* consumed as border-border, * { @apply border-border } */
  --card:      {H S% L%}   /* consumed as bg-card */
  --background:{H S% L%}   /* consumed as bg-background on body and main */
}
```

From client/tailwind.config.js (already correct — do NOT change structure):
```js
colors: {
  primary: { DEFAULT: "hsl(var(--primary))", foreground: "hsl(var(--primary-foreground))" },
  muted:   { DEFAULT: "hsl(var(--muted))",   foreground: "hsl(var(--muted-foreground))" },
  // ... all tokens wired this way
}
```
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update index.css colour tokens for B2B palette</name>
  <files>client/src/index.css</files>
  <action>
    Update ONLY the HSL triplet values in the `:root` block of `client/src/index.css`.
    Do NOT change any class names, property names, structure, or comments outside the token values.
    Do NOT touch any CSS rules below the `:root` block (body, h1–h3, scrollbar, focus-visible).

    Apply the following B2B-appropriate palette direction (care.com professional, trust-inspiring):

    ```css
    :root {
      /* Backgrounds — keep warm off-white page; clean white cards */
      --background:        210 20% 98%;     /* very slightly cooler off-white */
      --foreground:        220 20% 16%;     /* slightly deeper slate for contrast */

      --card:              0 0% 100%;       /* keep pure white cards */
      --card-foreground:   220 20% 16%;

      --popover:           0 0% 100%;
      --popover-foreground: 220 20% 16%;

      /* Primary — shift to deeper authority blue for B2B trust
         #1254a4 range: deep, readable, professional healthcare blue */
      --primary:           213 62% 36%;
      --primary-foreground: 0 0% 100%;

      /* Secondary — cooler light grey, cleaner for B2B */
      --secondary:         215 25% 91%;
      --secondary-foreground: 220 20% 32%;

      /* Muted — slightly cooler/cleaner */
      --muted:             215 20% 94%;
      --muted-foreground:  220 12% 46%;

      /* Accent — aligned with secondary */
      --accent:            215 25% 91%;
      --accent-foreground: 220 20% 22%;

      /* Status — keep existing (these are correct) */
      --destructive:       0 65% 48%;
      --destructive-foreground: 0 0% 100%;

      /* Borders — slightly cooler for B2B crispness */
      --border:            215 20% 86%;
      --input:             215 20% 86%;
      --ring:              213 62% 36%;    /* matches new primary */

      --radius: 0.75rem;                   /* unchanged */
    }
    ```

    After making the change, run the build to confirm no errors:
    `cd client && npm run build`

    Also run type check:
    `cd client && npx tsc --noEmit`

    Both must exit without errors.
  </action>
  <verify>
    <automated>cd /Users/rosekaron/Desktop/Kalinga/assistansportal/client && npm run build 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5</automated>
    Build must complete without errors. TypeScript must pass without errors.
    Confirm token update: `grep "\-\-primary:" client/src/index.css` must show `213 62% 36%`.
  </verify>
  <done>
    Build exits 0. TypeScript passes. `--primary` is `213 62% 36%`. `--ring` matches `--primary`.
    No other structural changes outside the :root HSL values.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Updated colour token palette. All pages now render with the new deeper primary blue and
    slightly cooler neutral palette. No component edits were made — the change propagated
    automatically through the token system.
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:5173 (dev server from Plan 01 must still be running, or run `npm run dev`).
    2. Log in and navigate to the Guardian Dashboard.
    3. Confirm the sidebar active nav item and logo icon show the deeper navy-blue (not the previous
       washed-out teal-blue).
    4. Confirm cards have a clean white surface on a warm off-white page background.
    5. Confirm focus rings on any input or button use the new primary colour.
    6. Navigate to the Assistant Dashboard and confirm the same primary colour is applied to the
       week strip's today highlight.
    7. Decision required: does the new primary blue feel professional and trust-inspiring for a
       B2B healthcare demo? Or does it need to shift (e.g. lighter, darker, more teal, more navy)?
  </how-to-verify>
  <resume-signal>
    Type "approved" if the palette direction is correct and we should proceed to layout redesign.
    Or describe the adjustment needed (e.g. "too dark", "too navy — shift back toward teal") and
    we will iterate on the token values before proceeding.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS build pipeline | Token values are static strings; no runtime input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.5-02-01 | Tampering | index.css | accept | Static CSS file in version control; no PII, no auth logic, no runtime attack surface |
</threat_model>

<verification>
- `npm run build` exits 0 in `client/`
- `npx tsc --noEmit` exits 0 in `client/`
- `--primary` token in index.css is `213 62% 36%` (or human-approved variant)
- `--ring` matches `--primary`
- Human confirms the new palette reads as professional B2B healthcare
</verification>

<success_criteria>
All pages globally use the updated B2B colour palette without any component-level edits. Human
approves the colour direction. Build and TypeScript are clean. Token system is ready to underpin
Plans 03 and 04 layout work.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-ui-overhaul/02.5-02-SUMMARY.md` summarising:
- Final HSL values for each token (especially --primary, approved by human checkpoint)
- Build and TypeScript status
- Any token values the human requested to adjust, and the final values used
</output>
