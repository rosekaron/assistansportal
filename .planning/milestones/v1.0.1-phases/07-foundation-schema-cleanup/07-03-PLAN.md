---
phase: 07-foundation-schema-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - server/src/routes/misc.ts
  - server/src/routes/assistant.ts
  - client/src/lib/api.ts
  - client/src/pages/Settings.tsx
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "GET/POST/DELETE /api/slots return 404 (routes deleted)"
    - "POST /api/assistant/self-book/:slotId returns 404 (route deleted)"
    - "GET /api/assistant/open-slots returns 404 (route deleted)"
    - "client/src/lib/api.ts has no slotsApi export, no assistantSelfApi.selfBook, no assistantSelfApi.openSlots"
    - "Settings.tsx has no Scheduling SectionLabel, no sched state, no saveSched mutation"
    - "sourceEnum 'self_book' value is still present in schema.ts (preserved per D-14 — used by clock.ts:182)"
  artifacts:
    - path: "server/src/routes/misc.ts"
      provides: "misc routes with /slots GET/POST/DELETE removed"
    - path: "server/src/routes/assistant.ts"
      provides: "assistant routes with /self-book and /open-slots removed"
    - path: "client/src/lib/api.ts"
      provides: "API client without slotsApi, selfBook, openSlots"
    - path: "client/src/pages/Settings.tsx"
      provides: "Settings page without Scheduling card, sched state, saveSched mutation"
  key_links:
    - from: "Settings.tsx"
      to: "server route coverage"
      via: "grep confirms zero references to removed routes from any UI file"
      pattern: "slotsApi|selfBook|openSlots"
    - from: "clock.ts:182"
      to: "sourceEnum 'self_book' value"
      via: "preserved per D-14"
      pattern: "source.*self_book"
---

<objective>
Atomically remove all dead scheduling scaffolding (CLEAN-01 + CLEAN-02 routes + CLEAN-03 client helpers) in a single coherent diff per D-13. The `openSlots` table is already removed from schema in Plan 01; this plan removes the API surface and UI surface that referenced it.

Purpose: Eliminate unreachable code so Phase 9 (salary slip) + future v1.2/v1.3 aren't fighting dead references. Single atomic commit per D-13 makes the diff clean-revertable.

Output: 4 files modified, 0 added, 0 new routes. Total deletion: ~30 server lines + ~5 client API lines + ~65 Settings.tsx lines (Scheduling card + sched state + saveSched).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-foundation-schema-cleanup/07-CONTEXT.md
@.planning/phases/07-foundation-schema-cleanup/07-UI-SPEC.md
@server/src/routes/misc.ts
@server/src/routes/assistant.ts
@client/src/lib/api.ts
@client/src/pages/Settings.tsx

<interfaces>
<!-- Exact locations to delete, extracted from scout pass -->

**server/src/routes/misc.ts — lines to remove (lines 15-38 inclusive):**
```ts
// ── Open Slots ────────────────────────────────────────────────
router.get("/slots", ...
router.post("/slots", ...
router.delete("/slots/:id", ...
```
Also remove the `openSlots` import from line 3: `import { openSlots, blocked, invites, settings } from "../db/schema";` → `import { blocked, invites, settings } from "../db/schema";`

**server/src/routes/assistant.ts — lines to remove:**
- Import `openSlots` from line 3: `import { entries, assistants, profile, openSlots, absences } from "../db/schema";` → `import { entries, assistants, profile, absences } from "../db/schema";`
- The whole `router.post("/self-book/:slotId", ...)` handler (lines 131-155)
- The whole `router.get("/open-slots", ...)` handler (lines 157-172)

**client/src/lib/api.ts — lines to remove:**
- `slotsApi` export (lines 84-89)
- `assistantSelfApi.openSlots` and `.selfBook` (lines 217-218) — keep the rest of `assistantSelfApi` intact

**client/src/pages/Settings.tsx — references to remove:**
- `const [sched, setSched] = useState({...})` at line 116-118
- Any `saveSched` useMutation definition (grep for `saveSched`)
- Any `useEffect` that sets `sched` from the settings GET response
- The entire Scheduling card block (line 618-667: `{/* ── 3. SCHEDULING ─`... through end of that `</Card>`)
- The `SectionLabel` "Scheduling" line (line 619)

**Imports to check in Settings.tsx:** if `Switch` or `Select` or `SelectTrigger/Content/Item` from `@/components/ui/controls` become unused, leave them (they will be re-needed by Plan 04). Do NOT reduce imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete server /api/slots and /api/assistant/self-book + /open-slots routes; drop openSlots imports</name>
  <files>server/src/routes/misc.ts, server/src/routes/assistant.ts</files>
  <read_first>
    - server/src/routes/misc.ts (full file)
    - server/src/routes/assistant.ts (full file)
    - server/src/routes/clock.ts (confirm line 182 still uses sourceEnum "self_book" — do NOT touch this file, just verify D-14 safeguard)
    - .planning/phases/07-foundation-schema-cleanup/07-CONTEXT.md (D-13, D-14, CLEAN-01, CLEAN-02)
  </read_first>
  <action>
**In `server/src/routes/misc.ts`:**
1. Change line 3 import: remove `openSlots` from the destructured import:
   - BEFORE: `import { openSlots, blocked, invites, settings } from "../db/schema";`
   - AFTER:  `import { blocked, invites, settings } from "../db/schema";`
2. Delete the entire "── Open Slots ──" block (the separator comment + the three handlers):
   - `router.get("/slots", ...)` — GET list
   - `router.post("/slots", ...)` — POST create
   - `router.delete("/slots/:id", ...)` — DELETE
3. Leave all other blocks (Blocked, Invites, Settings, Rates) exactly as-is.

**In `server/src/routes/assistant.ts`:**
1. Change line 3 import: remove `openSlots`:
   - BEFORE: `import { entries, assistants, profile, openSlots, absences } from "../db/schema";`
   - AFTER:  `import { entries, assistants, profile, absences } from "../db/schema";`
2. Delete the entire `router.post("/self-book/:slotId", ...)` handler.
3. Delete the entire `router.get("/open-slots", ...)` handler.
4. Leave `router.get("/me")`, `router.get("/entries")`, `router.get("/schedule")`, `router.put("/entries/:id/accept")`, `router.put("/entries/:id/reject")`, `router.put("/entries/:id/submit-report")` exactly as-is.

**Do NOT touch `server/src/routes/clock.ts`** — `sourceEnum "self_book"` remains in schema (Plan 01 preserved it) and clock.ts:182 still uses it as a clock-origin marker per D-14.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - ! grep -q 'router\\.(get|post|delete)\\("/slots' server/src/routes/misc.ts
    - ! grep -q 'router\\.(get|post|delete)\\("/slots' server/src/routes/misc.ts
    - ! grep -q "openSlots" server/src/routes/misc.ts
    - ! grep -q "self-book" server/src/routes/assistant.ts
    - ! grep -q "open-slots" server/src/routes/assistant.ts
    - ! grep -q "openSlots" server/src/routes/assistant.ts
    - grep -q 'router\\.get\\("/me"' server/src/routes/assistant.ts       (existing preserved)
    - grep -q 'router\\.(get|post)\\("/blocked' server/src/routes/misc.ts (existing preserved)
    - grep -q "self_book" server/src/routes/clock.ts                       (D-14 safeguard — clock.ts source enum usage preserved)
    - grep -q '"self_book"' server/src/db/schema.ts                        (D-14 safeguard — sourceEnum value preserved)
    - `cd server && npx tsc --noEmit` completes with zero errors
  </acceptance_criteria>
  <done>Both server route files purged of scheduling dead code; imports updated; TypeScript clean; clock.ts + sourceEnum untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove slotsApi, assistantSelfApi.selfBook, assistantSelfApi.openSlots from client api.ts</name>
  <files>client/src/lib/api.ts</files>
  <read_first>
    - client/src/lib/api.ts (full file)
    - .planning/phases/07-foundation-schema-cleanup/07-CONTEXT.md (CLEAN-03 definition, D-13 atomic scope)
  </read_first>
  <action>
1. **Delete the entire `slotsApi` export block** (currently lines 84-89):
```ts
// Slots
export const slotsApi = {
  list:   () => api.get("/slots"),
  create: (data: Record<string, unknown>) => api.post("/slots", data),
  delete: (id: string) => api.delete(`/slots/${id}`),
};
```

2. **Inside `assistantSelfApi` (currently lines 210-219), delete the `openSlots` and `selfBook` entries**:
   - Remove: `openSlots:    () => api.get("/assistant/open-slots"),`
   - Remove: `selfBook:     (slotId: string) => api.post(\`/assistant/self-book/${slotId}\`),`
   - Keep the other fields (me, entries, schedule, accept, reject, submitReport) exactly as-is.

3. Leave all other API namespace objects (authApi, profileApi, assistantsApi, entriesApi, blockedApi, invitesApi, settingsApi, pdfApi, costsApi, gcalApi, ratesApi, absenceApi, payrollApi, paymentsApi, clockApi, guardianLinksApi) unchanged.

4. No import changes needed (axios import is used by every remaining API).
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - ! grep -q "slotsApi" client/src/lib/api.ts
    - ! grep -q "selfBook" client/src/lib/api.ts
    - ! grep -q 'openSlots:' client/src/lib/api.ts
    - grep -q "assistantSelfApi" client/src/lib/api.ts           (object still exported)
    - grep -q "blockedApi" client/src/lib/api.ts                 (other namespaces preserved)
    - grep -q "assistantsApi" client/src/lib/api.ts              (different object preserved)
    - `cd client && npx tsc --noEmit` completes with zero errors
  </acceptance_criteria>
  <done>slotsApi gone; assistantSelfApi still exists but without selfBook/openSlots; TypeScript clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove Scheduling card + sched state + saveSched mutation from Settings.tsx</name>
  <files>client/src/pages/Settings.tsx</files>
  <read_first>
    - client/src/pages/Settings.tsx (full file — understand where sched is read/written/rendered)
    - .planning/phases/07-foundation-schema-cleanup/07-CONTEXT.md (CLEAN-01, D-13)
    - .planning/phases/07-foundation-schema-cleanup/07-UI-SPEC.md §"Scheduling card removal (CLEAN-01)" (confirms zero replacement UI; page self-heals via mt-8 gap)
  </read_first>
  <action>
Perform a single coherent edit pass on `client/src/pages/Settings.tsx`:

1. **Delete `sched` useState hook** (line 116-118):
```ts
const [sched, setSched] = useState({
  allowSelfBook: true, selfBookApproval: "require-approval", bookingWindowDays: "14",
});
```

2. **Delete any `useEffect` that syncs `sched` from settings GET response** (grep for `setSched(` inside a `useEffect` body — remove only that setSched call or its wrapping useEffect if that's the only thing inside it).

3. **Delete the `saveSched` useMutation** (grep for `saveSched` — it's a useMutation that PUTs `sched` state to `settingsApi.update`). Remove the entire mutation declaration.

4. **Delete the entire Scheduling card block** in JSX (lines 618-667 currently):
```tsx
{/* ── 3. SCHEDULING ──────────────────────────────────────────────────── */}
<SectionLabel>Scheduling</SectionLabel>
<Card className="mb-6">
  ... (all 3 ToggleRow + booking-window Select internals)
</Card>
```

5. **Verify remaining sections flow correctly:** the JSX should go from Section 2 (Integrations / GCal card) directly to Section 4 (Payroll rates card) without the Scheduling block in between. The `mt-8 mb-3` gap on the "Payroll rates" SectionLabel (line 670) provides natural vertical rhythm — no additional spacing adjustment needed per UI-SPEC.md "Page vertical rhythm self-heals".

6. **Leave all imports unchanged** — `Switch`, `Select`, `SelectTrigger`, `SelectContent`, `SelectItem` are still needed for Plan 04 (new Anställning & ekonomi section uses Select for tax_scheme, ToggleRow still used for dubbel_assistans_approved and patient_requires_representative). Removing imports and re-adding them in Plan 04 creates unnecessary diff churn.

7. **Do NOT renumber the section comments** (e.g. leave "5. ACCOUNT" header even though Scheduling was "3"). Renumbering is cosmetic and would enlarge diff unnecessarily. Plan 04 will touch this block and can address comment numbering if needed.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - ! grep -q "saveSched" client/src/pages/Settings.tsx
    - ! grep -qE "const \\[sched, setSched\\]" client/src/pages/Settings.tsx
    - ! grep -q "allowSelfBook" client/src/pages/Settings.tsx
    - ! grep -q "selfBookApproval" client/src/pages/Settings.tsx
    - ! grep -q "bookingWindowDays" client/src/pages/Settings.tsx
    - ! grep -q "SectionLabel>Scheduling" client/src/pages/Settings.tsx
    - grep -q "SectionLabel" client/src/pages/Settings.tsx             (primitive still imported + used elsewhere)
    - grep -q "Payroll rates" client/src/pages/Settings.tsx            (next section preserved)
    - grep -q "Integrations" client/src/pages/Settings.tsx || grep -q "Google Calendar" client/src/pages/Settings.tsx   (prev section preserved)
    - `cd client && npx tsc --noEmit` completes with zero errors
  </acceptance_criteria>
  <done>Scheduling card + sched state + saveSched mutation all removed; adjacent sections flow correctly; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → removed endpoints | Any external client still hitting `/api/slots`, `/api/assistant/self-book/:id`, or `/api/assistant/open-slots` will now receive 404 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Denial of Service | External client (e.g. bookmarked page, third-party script) calls removed route and receives 404 | accept | CONTEXT.md scout verified: no client code references these routes (slotsApi/selfBook/openSlots are the only call sites and they are removed in Task 2). No external API consumers exist (single-user anhörig app). 404 is safe behavior. |
| T-07-03-02 | Elevation of Privilege | Stale middleware or auth rule references a deleted path and fails-open | mitigate | Scout verified no middleware keys off `/slots` or `/self-book` paths. `requireAuth` + `requireAssistantAccess` apply at router-level, not path-level. Verification grep: `grep -E "slots|self-book|open-slots" server/src/middleware/*.ts` returns empty. |
| T-07-03-03 | Tampering | `sourceEnum "self_book"` value accidentally deleted (would break clock.ts) | mitigate | Explicit acceptance criterion: `grep -q '"self_book"' server/src/db/schema.ts` must pass. Task 1 action text + D-14 callouts make this a hard do-not-touch rule. Plan 01 also preserves the enum. |
| T-07-03-04 | Information Disclosure | Leftover orphan rows in `settings` with dead keys (allow_self_book, etc.) | accept | CONTEXT.md D-13: "won't harm if left — just orphans". No production secrets in these values ("true"/"14"/"require-approval"). A future `DELETE FROM settings WHERE key IN (...)` script could be added but is not required for Phase 7 verification. |
| T-07-03-05 | Tampering | `openSlots` table still referenced somewhere after drop → runtime crash | mitigate | Plan 01 already dropped table from schema. Task 1 acceptance criteria: `grep` ensures zero `openSlots` references in misc.ts and assistant.ts. Run `grep -r "openSlots" server/src/` after Task 1 → should return nothing. |
</threat_model>

<verification>
After execution: `grep -rE "slotsApi|selfBook|openSlots|saveSched|allowSelfBook" client/src server/src` returns zero matches. `grep -rE "self_book" server/src` returns only clock.ts (the legitimate usage). `cd client && npm run build` completes. `cd server && npm run build` completes.
</verification>

<success_criteria>
1. Zero references to `slotsApi`, `selfBook`, `openSlots` in client code
2. Zero references to `/api/slots`, `/api/assistant/self-book`, `/api/assistant/open-slots` in server code
3. Zero references to `sched`, `saveSched`, `allowSelfBook` in Settings.tsx
4. `sourceEnum "self_book"` still present in schema.ts (D-14 safeguard)
5. clock.ts untouched and still uses source "self_book" (D-14 safeguard)
6. Both client and server typecheck clean
7. Page renders correctly with Integrations → Payroll rates → Account flowing naturally
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-schema-cleanup/07-03-SUMMARY.md` documenting: exact line ranges removed per file, grep-verification results for D-14 safeguard (self_book value preserved in schema.ts and clock.ts), and confirmation that Settings page still renders without layout break.
</output>
