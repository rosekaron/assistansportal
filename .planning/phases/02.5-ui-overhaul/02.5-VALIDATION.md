---
phase: 2.5
slug: ui-overhaul
status: ready
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-07
---

# Phase 2.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (already installed — Phase 1 setup) |
| **Config file** | `client/vite.config.ts` (Vitest uses same config via `npm run test`) |
| **Quick run command** | `cd client && npx vitest run --reporter=dot` |
| **Full suite command** | `cd client && npm run test` |
| **Estimated runtime** | ~2.5 seconds |

---

## Sampling Rate

- **After every task commit:** `cd client && npm run build` (build must complete without errors)
- **After every plan wave:** `cd client && npx tsc --noEmit` (no TypeScript regressions)
- **Before `/gsd-verify-work`:** Build green + visual confirmation in browser (see Manual Verifications below)
- **Max feedback latency:** ~5 seconds (build smoke test)

---

## Per-Task Verification Map

| Task ID  | Plan | Wave | Requirement | Threat Ref   | Secure Behavior           | Test Type    | Automated Command                                                    | File Exists | Status     |
|----------|------|------|-------------|--------------|---------------------------|--------------|----------------------------------------------------------------------|-------------|------------|
| 2.5-01-1 | 01   | 1    | —           | T-2.5-01-01  | N/A — build config only   | build smoke  | `cd client && npm run build 2>&1 \| tail -5`                         | ✅           | ⬜ pending |
| 2.5-02-1 | 02   | 2    | —           | T-2.5-02-01  | N/A — CSS token only      | build + tsc  | `cd client && npm run build && npx tsc --noEmit`                     | ✅           | ⬜ pending |
| 2.5-03-1 | 03   | 3    | —           | T-2.5-03-02  | N/A — UI class strings    | tsc          | `cd client && npx tsc --noEmit`                                      | ✅           | ⬜ pending |
| 2.5-03-2 | 03   | 3    | —           | T-2.5-03-02  | N/A — UI class strings    | tsc          | `cd client && npx tsc --noEmit`                                      | ✅           | ⬜ pending |
| 2.5-04-1 | 04   | 3    | —           | T-2.5-04-01  | N/A — UI class strings    | tsc          | `cd client && npx tsc --noEmit`                                      | ✅           | ⬜ pending |
| 2.5-05-1 | 05   | 4    | —           | T-2.5-05-01  | Auth flow unchanged       | tsc          | `cd client && npx tsc --noEmit`                                      | ✅           | ⬜ pending |
| 2.5-05-2 | 05   | 4    | —           | T-2.5-05-02  | N/A — UI class strings    | build + tsc  | `cd client && npx tsc --noEmit && npm run build 2>&1 \| tail -5`     | ✅           | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

None. Phase 2.5 has no functional requirements and no new test files needed. The Vitest
suite from Phase 1 provides smoke coverage for existing functionality.

Existing infrastructure covers all phase requirements: build smoke test + TypeScript check
+ browser visual inspection.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tailwind utility classes render in browser | Plan 01 success criteria | No screenshot diffing infrastructure; requires browser console check | Open http://localhost:5173 in browser. Open console. Run `document.styleSheets[0].cssRules.length` — must be > 5000. Also run `getComputedStyle(document.body).backgroundColor` — must be non-empty. |
| B2B palette looks professional and trust-inspiring | Plan 02 success criteria | Subjective design quality; cannot be automated | Log in as guardian. Navigate to /dashboard. Check sidebar active state uses deep blue primary. Check card surfaces are white on off-white page background. |
| Guardian sidebar and dashboard look enterprise-appropriate | Plan 03 success criteria | Subjective design quality | Navigate all guardian pages. Verify sidebar active state, logo, weekly hours widget, and dashboard card hierarchy. |
| Assistant dashboard matches light theme | Plan 04 success criteria | Subjective; dark-mode artefact detection | Log in as assistant. Verify no amber-950, blue-950, or similar dark artefacts visible. Week strip, banners, and shift cards should all be light-theme. |
| All pages visually consistent end-to-end | Plan 05 success criteria | Requires full page traversal | Navigate through all 8 pages. Verify Login, Leave, Reports, Calendar, Assistants, Settings all match Dashboard visual language. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify (build smoke or tsc)
- [x] Sampling continuity: every task has a build or tsc check
- [x] Wave 0 not needed — existing infrastructure sufficient
- [x] No watch-mode flags in any verify command
- [x] Feedback latency < 5s (build exits in ~3-4s for this project size)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready 2026-04-07
